{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "JwtSettings": {
        "Secret": "", //Use JSON user-secrets for everything instead of just this one.
        "ExpiryMinutes": 60,
        "Issuer": "BankApplication",
        "Audience": "BankApplication"
    },
    "ConnectionStrings": {
        "SqlServer": ""
    }
}

